name: Trivy scan

# Сканирование образа, файловой системы и Dockerfile.
# Отчёты загружаются в GitHub Security (SARIF), а находки уровня CRITICAL/HIGH
# роняют прогон: это «ворота», а не просто отчёт.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

concurrency:
  group: trivy-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  image:
    name: Образ (${{ matrix.tool }})
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      security-events: write
    strategy:
      # Падение сканирования одного образа не должно прятать результат второго.
      fail-fast: false
      matrix:
        include:
          - tool: cio
            dir: Container image optimizer
            image: cio:scan
            sarif: trivy-image-cio.sarif
          - tool: sqlbrc
            dir: SQL backup restore clone
            image: sqlbrc:scan
            sarif: trivy-image-sqlbrc.sarif
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1
      - name: Собрать образ
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: ${{ matrix.dir }}
          push: false
          load: true
          tags: ${{ matrix.image }}
      - name: Trivy (SARIF)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ matrix.image }}
          format: sarif
          output: ${{ matrix.sarif }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "0"
      - name: Загрузить отчёт в GitHub Security
        # В PR из форка токен не имеет права писать в Security: загрузку пропускаем,
        # сама проверка ниже всё равно выполняется.
        if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        uses: github/codeql-action/upload-sarif@1c5b675653bb5c22dbe9b12b556ec555138e09fd # v4.38.1
        with:
          sarif_file: ${{ matrix.sarif }}
          category: trivy-image-${{ matrix.tool }}
      - name: Trivy — ворота по уязвимостям образа cio
        # Исключения (.trivyignore) есть только у образа cio: они касаются
        # встроенного в него сканера Trivy, у образа sqlbrc исключений нет.
        if: matrix.tool == 'cio'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ matrix.image }}
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          # Что именно исключено и почему, описано в .trivyignore; срок годности
          # (exp) заставляет перепроверить список.
          trivyignores: .trivyignore
          exit-code: "1"
      - name: Trivy — ворота по уязвимостям образа sqlbrc
        # Код сервиса и базовый образ проверяются без исключений.
        if: matrix.tool == 'sqlbrc'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ matrix.image }}
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"

  filesystem:
    name: Файловая система и Dockerfile (${{ matrix.tool }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
    strategy:
      # Падение сканирования одного инструмента не должно прятать результат второго.
      fail-fast: false
      matrix:
        include:
          - tool: cio
            dir: Container image optimizer
          - tool: sqlbrc
            dir: SQL backup restore clone
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Trivy fs (SARIF)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: fs
          scan-ref: ${{ matrix.dir }}
          scanners: vuln,secret,misconfig
          format: sarif
          output: trivy-fs.sarif
          severity: CRITICAL,HIGH
          exit-code: "0"
      - name: Загрузить отчёт в GitHub Security
        if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
        uses: github/codeql-action/upload-sarif@1c5b675653bb5c22dbe9b12b556ec555138e09fd # v4.38.1
        with:
          sarif_file: trivy-fs.sarif
          # Категория уникальна для инструмента: иначе отчёты перекрывали бы друг друга.
          category: trivy-fs-${{ matrix.tool }}
      - name: Trivy — ворота по зависимостям, секретам и Dockerfile
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: fs
          scan-ref: ${{ matrix.dir }}
          scanners: vuln,secret,misconfig
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"

