# Что не попадает в контекст сборки образа:
#   * служебные каталоги разработки (в Git их нет, но локально они есть);
#   * артефакты сборки и покрытия;
#   * данные тестов: они нужны только `go test`, а не сборке бинаря.
.git
.github
.clinerules
.work
bin
dist
coverage.out
*.exe
*.log
testdata
